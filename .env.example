# Local only — copy to .env (never commit .env)
TRAVELPAYOUTS_TOKEN=your_travelpayouts_api_token

# Public Mapbox token (restrict by URL in Mapbox dashboard)
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Optional
# ORIGINS=ATL,DXB,HND,DFW,PVG,LHR,DEN,IST,CAN,ORD,CDG,FRA,AMS
# MONTHS_AHEAD=6
